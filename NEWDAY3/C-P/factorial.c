#include<stdio.h>
int main()
{
    int a,n,fact=1;
    printf("Enter a number to print fctorial:",n);
    scanf("%d",&n);
    for(a=n;a<=n;a--)
    {
            fact=fact*a;
    }
    printf("factorial  is %d.\n",fact);
    return 0;
}
